{
  "name": "loldabang",
  "version": "1.0.0",
  "description": "LoL Custom Match Tool",
  "main": "index.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r dist/* ../public/",
    "vercel-build": "cd frontend && npm install && npm run build && cp -r dist/* ../public/",
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm run preview",
    "preview": "cd frontend && npm run preview"
  },
  "keywords": ["lol", "match", "tool"],
  "author": "",
  "license": "ISC"
}